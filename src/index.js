import React from "react";
import ReactDOM from "react-dom/client";
import CRMView from "./App";
import "./styles.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <CRMView />
  </React.StrictMode>
);